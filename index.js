const name = "Susan"; // Change "Joe" to "Susan" to pass the first test
const height = 30; // Change 74 to a value less than 40 to pass the second test
const message = `${name} is ${height} inches tall`;

module.exports = { name, height, message };
console.log(message); // This will log "Susan is 30 inches tall"
